{
    "name": "apt-tennis",
    "version": "1.0.0",
    "description": "",
    "main": "atp.js",
    "scripts": {
        "git-backup": "branch=$(git branch --show-current) && if [ -n \"$(git status --short)\" ]; then git add -A && git commit -m 'backup'; fi && git push origin \"$branch\" && git push --force origin HEAD:refs/heads/backup/$branch",
        "git-commit": "git add -A && git commit -m '-' && git push",
        "git-delete-backup": "branch=$(git branch --show-current) && git push origin --delete \"backup/$branch\" && git fetch origin --prune",
        "git-restore": "branch=$(git branch --show-current) && git fetch origin && git reset --hard origin/backup/$branch && git push --force-with-lease origin \"$branch\"",
        "show-atp-log": "ssh pi@pi-kato 'tail -f ~/atp-tennis/atp.log'",
        "reboot-server": "ssh pi@pi-kato 'sudo reboot'",
        "restart-service": "ssh root@pi-kato 'cd /home/pi/atp-tennis && git pull && pm2 restart atp-service'",
        "restart-import": "ssh root@pi-kato 'cd /home/pi/atp-tennis && git pull && pm2 restart atp-import'",
        "run serve": "./atp.js serve",
        "update-wikipedia": "node helpers/update-player-wikipedia.js",
        "goto-github": "open https://github.com/meg768/atp-tennis"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "body-parser": "^2.2.0",
        "compression": "^1.8.0",
        "cors": "^2.8.5",
        "dotenv": "^16.4.7",
        "express": "^5.1.0",
        "jsonpath": "^1.1.1",
        "mysql": "^2.18.1",
        "yargs": "^17.7.2",
        "yow": "^1.0.118"
    },
    "devDependencies": {
        "eslint": "^9.19.0",
        "prettier": "^3.4.2",
        "prettier-plugin-sql": "^0.18.1"
    }
}
